{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "players": {
      "$playerId": {
        ".read": true,
        ".write": "auth != null || newData.child('admin').val() === true"
      }
    }
  }
}
